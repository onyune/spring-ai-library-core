# 04. @Tool 애노테이션 방식: 더 간결한 Function Calling

## 학습 전제조건

이 문서를 학습하기 전에 다음 내용을 알고 있으면 도움이 됩니다:
- **[02. 단일 Function 구현]** 학습 완료: FunctionToolCallback.builder() 방식 이해
- **[03. 복수 Function 조합]** 학습 완료: 다중 Function 등록 방법 이해
- **Spring Boot 기초**: @Component, @Service 패턴 이해
- **Java 애노테이션**: @Annotation 기반 메타프로그래밍 개념

---

## 1. 개요

이 문서에서는 Spring AI 1.1.2의 **`@Tool` 애노테이션**을 사용하여 더 간결하게 Function Calling을 구현하는 방법을 배웁니다.

**학습 목표**:
- `@Tool` 애노테이션의 장점 이해
- `@ToolParam`으로 파라미터 설명 추가
- FunctionToolCallback.builder()와의 차이 이해
- 더 간결한 코드로 Function 구현

**구현할 것**:
- `@Tool` 기반 LibraryTools
- `@Tool` 방식 AiOrchestrationService
- Chat Memory 추가 (선택)

---

## 2. @Tool 애노테이션 방식 소개

### 2.1. 두 가지 방식 비교

#### 방식 1: FunctionToolCallback.builder() (기존)

```java
// LibraryFunctions.java
@Component
public class LibraryFunctions {
    private final BookSearchService bookSearchService;

    public Function<BookSearchRequest, BookSearchResult> searchBooks() {
        return request -> {
            return bookSearchService.searchByTitle(request.query());
        };
    }
}

// AiOrchestrationService.java
FunctionToolCallback<BookSearchRequest, BookSearchResult> searchBooksTool =
    FunctionToolCallback.builder("SearchBooks", libraryFunctions.searchBooks())
        .description("도서관 시스템에서 도서를 검색합니다.")
        .inputType(BookSearchRequest.class)
        .build();

ChatClient chatClient = ChatClient.builder(chatModel)
    .defaultToolCallbacks(searchBooksTool)
    .build();
```

#### 방식 2: @Tool 애노테이션 (새로운 방식)

```java
// LibraryTools.java
@Component
public class LibraryTools {
    private final BookSearchService bookSearchService;

    @Tool(description = "도서관 시스템에서 도서를 검색합니다.")
    public List<BookSearchResult> searchBooks(
        @ToolParam(description = "검색할 도서 제목") String title) {
        return bookSearchService.searchByTitle(title);
    }
}

// AiOrchestrationService.java
ChatClient chatClient = ChatClient.builder(chatModel)
    .defaultTools(libraryTools)  // ✅ 자동 등록
    .build();
```

### 2.2. 장점 비교

| 특징 | FunctionToolCallback.builder() | @Tool 애노테이션 |
|------|------------------------------|------------------|
| **코드 간결성** | ⚠️ Builder 패턴으로 코드 길어짐 | ✅ 애노테이션으로 간결 |
| **파라미터 설명** | ⚠️ DTO 클래스 필요 | ✅ @ToolParam으로 직접 설명 |
| **타입 안전성** | ✅ 강력한 타입 안전성 | ✅ 강력한 타입 안전성 |
| **자동 등록** | ❌ 수동 등록 필요 | ✅ Spring이 자동 스캔 |
| **순환 의존성** | ✅ 프로그래밍 방식으로 회피 가능 | ⚠️ Bean 주입 시 주의 필요 |
| **코드 가독성** | ⚠️ 설정 코드 분산 | ✅ 메서드 위에 바로 정의 |

---

## 3. @Tool 애노테이션 구현

### STEP 1: 의존성 확인

**pom.xml** - Spring AI 1.1.2 BOM이 필요합니다:

```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-bom</artifactId>
            <version>1.1.2</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
```

---

### STEP 2: LibraryTools 구현

**LibraryTools.java** (`ai/function/LibraryTools.java`)

```java
package com.nhnacademy.library.ai.function;

import com.nhnacademy.library.core.book.service.search.BookSearchService;
import com.nhnacademy.library.external.opennaru.client.LibraryInfoNaruApiClient;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.ai.tool.annotation.Tool;
import org.springframework.ai.tool.annotation.ToolParam;
import org.springframework.stereotype.Component;

import java.util.List;

/**
 * Library Tools - @Tool 애노테이션 방식
 * <p>
 * Spring AI 1.1.2의 @Tool 애노테이션을 사용하여
 * 도서관 관련 기능을 LLM에 제공합니다.
 * </p>
 *
 * <p><b>장점:</b></p>
 * <ul>
 *   <li>애노테이션 기반으로 코드 간결</li>
 *   <li>@ToolParam으로 파라미터 설명 직접 제공</li>
 *   <li>Spring이 자동으로 스캔하여 ChatClient에 등록</li>
 * </ul>
 */
@Component
@RequiredArgsConstructor
@Slf4j
public class LibraryTools {

    private final BookSearchService bookSearchService;
    private final LibraryInfoNaruApiClient libraryInfoNaruApiClient;

    /**
     * 도서 검색 Tool
     */
    @Tool(
        description = """
            도서관 시스템에서 도서를 검색합니다.
            제목으로 검색할 수 있으며, 검색어를 받아 도서 목록과 ISBN을 반환합니다.

            **검색 대상:**
            - 도서관정보나루 API (전국 도서관 소장 도서)
            - 내부 DB (등록된 도서 정보)

            **반환 정보:**
            - 도서 제목
            - 저자명
            - 출판사
            - ISBN (13자리)
            """
    )
    public List<BookSearchResult> searchBooks(
        @ToolParam(description = "검색할 도서 제목 (예: 토비의 스프링, 자바의 정석)")
        String title) {

        log.info("[@Tool] searchBooks 호출: title={}", title);

        try {
            // 도서관정보나루 API 검색
            var libraryBooks = libraryInfoNaruApiClient.searchBooksByTitle(title);

            log.info("[@Tool] searchBooks 완료: {}권 검색됨", libraryBooks.size());

            return libraryBooks.stream()
                .map(book -> new BookSearchResult(
                    book.title(),
                    book.author(),
                    book.publisher(),
                    book.isbn13()
                ))
                .toList();

        } catch (Exception e) {
            log.error("[@Tool] searchBooks 실패: title={}", title, e);
            return List.of();
        }
    }

    /**
     * 도서관 검색 Tool
     */
    @Tool(
        description = """
            지역별 도서관을 검색합니다.
            지역 코드(숫자)를 받아 해당 지역의 도서관 목록을 반환합니다.

            **⚠️ 매우 중요:**
            - 반드시 숫자 지역 코드를 사용하세요.
            - 한글 지역명(광주, 서울 등)을 사용하면 API 에러가 발생합니다.

            **지역 코드 예시:**
            - 서울=11, 부산=21, 대구=22, 인천=23
            - 광주=29, 대전=25, 울산=26, 세종=36
            - 경기=31, 강원=32, 충북=33, 충남=34
            """
    )
    public List<LibrarySearchResult> searchLibraries(
        @ToolParam(description = "지역 코드 (숫자, 예: 11=서울, 29=광주)")
        String region) {

        log.info("[@Tool] searchLibraries 호출: region={}", region);

        try {
            var libraries = libraryInfoNaruApiClient.searchLibrariesByRegion(region);

            log.info("[@Tool] searchLibraries 완료: {}개 도서관 검색됨", libraries.size());

            return libraries.stream()
                .map(lib -> new LibrarySearchResult(
                    lib.libName(),
                    lib.libCode(),
                    lib.address(),
                    lib.tel()
                ))
                .toList();

        } catch (Exception e) {
            log.error("[@Tool] searchLibraries 실패: region={}", region, e);
            return List.of();
        }
    }

    /**
     * 도서 소장 여부 확인 Tool
     */
    @Tool(
        description = """
            특정 도서관에 도서가 소장되어 있는지 확인합니다.
            도서관 코드(libCode)와 ISBN을 받아 소장 여부와 대출 가능 여부를 반환합니다.
            """
    )
    public BookExistResult checkBookExists(
        @ToolParam(description = "도서관 코드 (예: 111001)")
        String libCode,

        @ToolParam(description = "ISBN 13자리 (예: 9788960777330)")
        String isbn13) {

        log.info("[@Tool] checkBookExists 호출: libCode={}, isbn={}", libCode, isbn13);

        try {
            var existInfo = libraryInfoNaruApiClient.checkBookExists(libCode, isbn13);

            log.info("[@Tool] checkBookExists 완료: hasBook={}, available={}",
                existInfo.hasBook(), existInfo.isAvailable());

            return new BookExistResult(
                existInfo.libName(),
                existInfo.hasBook(),
                existInfo.isAvailable(),
                existInfo.loanAvailable()
            );

        } catch (Exception e) {
            log.error("[@Tool] checkBookExists 실패", e);
            return new BookExistResult(null, false, false, false);
        }
    }

    // ===== Result DTOs =====

    public record BookSearchResult(
        String title,
        String author,
        String publisher,
        String isbn
    ) {}

    public record LibrarySearchResult(
        String libraryName,
        String libraryCode,
        String address,
        String phoneNumber
    ) {}

    public record BookExistResult(
        String libraryName,
        boolean hasBook,
        boolean isAvailable,
        boolean loanAvailable
    ) {}
}
```

### 코드 설명

**1. @Tool 애노테이션**
- 메서드를 LLM이 호출할 수 있는 Tool로 등록
- `description`에 Tool의 용도를 상세히 설명
- Text Block (`"""`)을 사용하여 여러 줄 설명 가능

**2. @ToolParam 애노테이션**
- 각 파라미터의 역할을 LLM에게 설명
- 파라미터의 예시를 제공하여 LLM이 올바른 값을 입력하도록 유도
- `required = false`로 선택적 파라미터 지정 가능

**3. Record 반환 타입**
- 불변 데이터 객체로 결과 반환
- Spring AI가 자동으로 JSON으로 변환하여 LLM에게 전달

**4. 로그**
- `[@Tool]` 접두사로 Tool 호출 로깅
- 디버깅 및 모니터링에 활용

---

## 4. AiOrchestrationService 개선

### 4.1. ToolBasedAiOrchestrationService

**ToolBasedAiOrchestrationService.java** (`ai/agent/orchestrator/ToolBasedAiOrchestrationService.java`)

```java
package com.nhnacademy.library.ai.agent.orchestrator;

import com.nhnacademy.library.ai.agent.QueryParserAgent;
import com.nhnacademy.library.ai.agent.dto.ParsedQuery;
import com.nhnacademy.library.ai.function.LibraryTools;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.model.ChatModel;
import org.springframework.ai.chat.prompt.ChatMemory;
import org.springframework.ai.chat.prompt.PromptChatMemoryAdvisor;
import org.springframework.stereotype.Service;

/**
 * AI Orchestration Service - @Tool 애노테이션 방식
 * <p>
 * Spring AI 1.1.2의 @Tool 애노테이션을 활용한 간결한 Function Calling 구현입니다.
 * 기존 FunctionToolCallback.builder() 방식보다 코드가 훨씬 간결합니다.
 * </p>
 */
@Service
@RequiredArgsConstructor
@Slf4j
public class ToolBasedAiOrchestrationService {

    private final ChatModel chatModel;
    private final QueryParserAgent queryParserAgent;
    private final LibraryTools libraryTools;
    private final ChatMemory chatMemory;  // 선택사항: 대화 기록 유지

    public String ask(String userMessage) {
        long startTime = System.currentTimeMillis();
        log.info("[MainAgent] 사용자 요청 수신: {}", userMessage);

        try {
            // 1. QueryParserAgent로 쿼리 파싱
            ParsedQuery parsed = queryParserAgent.parse(userMessage);

            if (!parsed.isValid()) {
                log.warn("[MainAgent] 파싱 실패: 유효하지 않은 쿼리");
                return "질문을 이해하지 못했습니다. 도서 검색, 도서관 조회 등으로 질문해주세요.";
            }

            // 2. ChatClient 생성 (@Tool 자동 등록)
            ChatClient chatClient = ChatClient.builder(chatModel)
                .defaultTools(libraryTools)  // ✅ @Tool 메서드 자동 등록
                .defaultAdvisors(
                    new PromptChatMemoryAdvisor(chatMemory)  // ✅ 대화 기록 유지
                )
                .build();

            // 3. System Prompt
            String systemPrompt = buildSystemPrompt(parsed);

            // 4. LLM 호출
            String response = chatClient.prompt()
                .system(systemPrompt)
                .user(userMessage)
                .call()
                .content();

            long totalElapsed = System.currentTimeMillis() - startTime;
            log.info("[MainAgent] 응답 생성 완료 (총 {}ms)", totalElapsed);

            return response;

        } catch (Exception e) {
            long elapsed = System.currentTimeMillis() - startTime;
            log.error("[MainAgent] 요청 처리 실패 ({}ms)", elapsed, e);
            return "요청 처리 중 오류가 발생했습니다. 다시 시도해주세요.";
        }
    }

    private String buildSystemPrompt(ParsedQuery parsed) {
        StringBuilder prompt = new StringBuilder();
        prompt.append("당신은 도서관 도우미 AI입니다.\n\n");

        if (parsed.hasBookName()) {
            prompt.append(String.format("검색 도서: %s\n\n", parsed.bookName()));
        }
        if (parsed.hasRegionName() && parsed.regionCode() != null) {
            prompt.append(String.format("**지역 정보:** %s (지역 코드: %s)\n", parsed.regionName(), parsed.regionCode()));
            prompt.append(String.format("**⚠️ 매우 중요:** searchLibraries 함수를 호출할 때 반드시 지역 코드 \"%s\"를 사용하세요.\n", parsed.regionCode()));
        }

        prompt.append("**중요한 지침:**\n");
        prompt.append("1. 반드시 한국어로 답변하세요.\n");
        prompt.append("2. Telegram 메신저에 맞춰 간단 명료하게 작성하세요.\n\n");

        return prompt.toString();
    }
}
```

### 4.2. ChatMemory 설정 (선택사항)

**application.properties**:

```properties
# Chat Memory 설정
spring.ai.chat.memory.enabled=true
spring.ai.chat.memory.in-memory.enabled=true
spring.ai.chat.memory.in-memory.conversation-history-size=10
```

또는 **@Configuration**:

```java
@Configuration
public class ChatMemoryConfig {

    @Bean
    public ChatMemory chatMemory() {
        return new InMemoryChatMemory();
    }
}
```

---

## 5. 방식 선택 가이드

### 5.1. FunctionToolCallback.builder()를 사용해야 하는 경우

1. **순환 의존성 문제가 있는 경우**
   - Tool과 Service가 서로를 참조하는 경우
   - 프로그래밍 방식으로 동적 Tool 생성이 필요한 경우

2. **동적 Tool 등록이 필요한 경우**
   - 런타임에 Tool 목록이 변경되는 경우
   - 조건에 따라 다른 Tool을 등록해야 하는 경우

3. **기존 코드와 호환성이 중요한 경우**
   - 이미 FunctionToolCallback 방식으로 구현된 경우
   - 대규모 리팩토링을 피하고 싶은 경우

### 5.2. @Tool 애노테이션을 사용해야 하는 경우

1. **코드 간결성이 중요한 경우**
   - 새로운 프로젝트를 시작하는 경우
   - 코드 가독성을 높이고 싶은 경우

2. **파라미터 설명이 중요한 경우**
   - LLM에게 파라미터의 의미를 명확히 전달하고 싶은 경우
   - 예시와 함께 설명을 제공하고 싶은 경우

3. **순환 의존성 문제가 없는 경우**
   - Tool이 Service에만 의존하고, 그 반대는 아닌 경우
   - 명확한 의존성 방향이 있는 경우

---

## 6. 실행 및 테스트

### 6.1. 애플리케이션 실행

```bash
./mvnw spring-boot:run
```

### 6.2. 테스트

**Telegram에서 메시지 전송**:
```
광주에서 토비의 스프링 빌릴까?
```

**기대 동작**:
1. QueryParserAgent가 쿼리 파싱
2. ChatClient가 @Tool 메서드 자동 등록
3. LLM이 적절한 Tool 선택 (searchBooks → searchLibraries → checkBookExists)
4. 결과 종합하여 응답 생성

**로그 확인**:
```log
[@Tool] searchBooks 호출: title=토비의 스프링
[@Tool] searchBooks 완료: 3권 검색됨
[@Tool] searchLibraries 호출: region=29
[@Tool] searchLibraries 완료: 15개 도서관 검색됨
[@Tool] checkBookExists 호출: libCode=111001, isbn=9788960777330
[@Tool] checkBookExists 완료: hasBook=true, available=true
```

---

## 7. 두 방식의 공존

**핵심**: 두 방식은 **동시에 사용 가능**합니다!

```java
ChatClient chatClient = ChatClient.builder(chatModel)
    .defaultTools(libraryTools)  // @Tool 애노테이션 기반 Tools
    .defaultToolCallbacks(       // FunctionToolCallback 기반 Tools
        legacyToolCallback,
        dynamicToolCallback
    )
    .build();
```

### 마이그레이션 전략

1. **Phase 1**: 기존 FunctionToolCallback 유지
2. **Phase 2**: 새로운 Tool은 @Tool 애노테이션으로 추가
3. **Phase 3**: 점진적으로 기존 Tool을 @Tool 방식으로 변경

---

## 8. 학습 체크리스트

다음 내용을 이해했는지 확인해 보세요:

### 기본 개념
- [ ] @Tool 애노테이션의 장점을 설명할 수 있다
- [ ] @ToolParam의 역할을 이해한다
- [ ] FunctionToolCallback.builder()와의 차이를 안다

### 구현 능력
- [ ] @Tool 애노테이션으로 Tool을 정의할 수 있다
- [ ] @ToolParam으로 파라미터 설명을 추가할 수 있다
- [ ] ChatClient에 @Tool을 자동 등록할 수 있다

### 실습 완료
- [ ] LibraryTools가 정상 작동한다
- [ ] ToolBasedAiOrchestrationService가 작동한다
- [ ] 로그를 통해 Tool 호출을 확인했다

---

## 9. 다음 단계

**다음 문서**: [05. A2A 구현](./05.a2a-implementation.md)

@Tool 애노테이션을 활용하여 Agent 간 통신(A2A)을 구현하는 방법을 배워봅시다!

---

## 참고 자료

- [Spring AI ChatClient API](https://docs.spring.io/spring-ai/reference/api/chatclient.html)
- [Spring AI Tools Reference](https://docs.spring.io/spring-ai/reference/api/tools.html)
- [Spring AI 1.1.2 New Features](https://blog.csdn.net/weixin_44058951/article/details/155541542)

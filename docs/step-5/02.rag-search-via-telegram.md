# Telegram을 통한 RAG 검색

## 개요

Step 2에서 구현한 RAG(Retrieval-Augmented Generation) 검색 기능을 Telegram Bot을 통해 사용할 수 있도록 연동합니다.

## 학습 목표

- 외부 Service 연동 경험
- 검색 결과 포맷팅
- 사용자 인터랙션 설계
- 이벤트 기반 비동기 처리 이해

## 아키텍처

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Telegram   │ ──▶ │    Bot      │ ──▶ │   RAG       │ ──▶ │   Event     │
│    User     │     │  Handler    │     │  Strategy   │     │  Listener   │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                            │                    │                    │
                            ▼                    ▼                    ▼
                     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
                     │  Response   │ ◀─  │  Book       │ ◀─  │    LLM      │
                     │  Formatter  │     │  Service    │     │  (Ollama)   │
                     └─────────────┘     └─────────────┘     └─────────────┘
```

---

## 핵심 개념

### 1. RAG 검색 흐름

```
사용자 입력 → Bot 수신 → RAG 검색 요청 → 캐시 확인 → 결과 포맷팅 → 응답 전송
                        ↓              ↓
                 BookSearchService  ← 이벤트 발행
                 (SearchType.RAG)      ↓
                        ↑         Async Task
                        └─────────▶ LLM 검증 및 캐싱
```

### 2. Strategic Caching 패턴

**최초 검색 (캐시 미스):**
1. 사용자가 "해리포터" 검색
2. 시맨틱 캐시 확인 → 없음
3. 하이브리드 결과 즉시 반환 (빠른 응답)
4. BookSearchEvent 발행
5. 백그라운드에서 LLM 검증 + 캐싱

**두 번째 검색 (캐시 히트):**
1. 사용자가 "해리포터" 재검색 또는 유사한 키워드 검색
2. 시맨틱 캐시 확인 → 있음!
3. LLM 검증된 결과 즉시 반환

### 3. 이벤트 기반 비동기 처리

**BookSearchEvent 발행:**
```java
// RagSearchStrategy.java
if (!request.isWarmUp()) {
    Optional<BookSearchResult> cachedResult = semanticCacheService.findSimilarResult(request);
    if (cachedResult.isPresent()) {
        return cachedResult.get();  // 캐시 있음 → 즉시 반환
    }

    // 캐시 없음 → 빠른 응답 + 백그라운드 처리
    eventPublisher.publishEvent(new BookSearchEvent(this, request.keyword()));
    return hybridSearchStrategy.search(pageable, request);
}
```

**BookSearchEventListener 처리:**
```java
@Async
@EventListener
public void handleBookSearchEvent(BookSearchEvent event) {
    // 별도 스레드에서 LLM 검증 실행
    bookSearchCacheService.warmUpRagCache(event.getKeyword());
}
```

### 4. LLM 검증 결과 반영

**검증 과정:**
1. 후보군 100개 추출 (RRF ≥ 0.02 필터링)
2. LLM에게 relevance 점수 (0~100) 부여 요청
3. relevance ≥ 50인 도서만 승인
4. relevance 기반 내림차순 정렬

**코드 예시:**
```java
// relevance ≥ 50 필터링
List<Long> llmApprovedIds = aiResponse.stream()
    .filter(r -> r.getRelevance() != null && r.getRelevance() >= 50)
    .map(BookAiRecommendationResponse::getId)
    .toList();

// relevance 기반 정렬
List<BookSearchResponse> llmApprovedBooks = retrievalResult.getBooks().getContent()
    .stream()
    .filter(b -> llmApprovedIds.contains(b.getId()))
    .sorted((b1, b2) -> {
        Integer r1 = relevanceMap.getOrDefault(b1.getId(), 0);
        Integer r2 = relevanceMap.getOrDefault(b2.getId(), 0);
        return r2.compareTo(r1);  // 내림차순
    })
    .toList();
```

### 5. 메시지 포맷팅 전략

Telegram 메시지는 **4096자 제한**이 있습니다.

**전략:**
- 최대 5개 결과 표시
- 제목, 저자, 출판사, 이미지 정보
- AI 추천 사유 (없으면 "-" 표시)
- 유사도, RRF 점수 표시
- Markdown 사용으로 가독성 향상

### 6. Command vs 일반 텍스트

**Command (`/`로 시작):**
- `/search 해리포터`
- 명시적인 검색 의도

**일반 텍스트:**
- `해리포터`
- 자연스러운 검색 경험 (별도 Command 불필요)

**구현:**
```java
if (messageText.startsWith("/")) {
    handleCommand(update, messageText);  // /search, /start, /help
} else {
    handleSearch(update, messageText);   // 일반 텍스트도 검색 처리
}
```

---

## 구현 가이드

### 1. RAG 검색 처리

```java
/**
 * 도서 검색 처리 (RAG)
 *
 * <p>RAG 검색을 수행하여 AI 추천 사유와 함께 도서를 추천합니다.
 * 캐싱된 추천 도서가 있으면 먼저 보여주고, 검색 결과를 표시합니다.
 */
private void handleSearch(Update update, String keyword) {
    Long chatId = update.getMessage().getChatId();

    try {
        // 1. RAG 검색 실행 (캐시 확인, LLM 추천 사유 생성 포함)
        Pageable pageable = PageRequest.of(0, 5);
        BookSearchRequest request = new BookSearchRequest(keyword, null, SearchType.RAG, null, false);
        BookSearchResult result = bookSearchService.searchBooks(pageable, request);

        // 2. 응답 전송 (이미지, 점수, AI 추천 사유 포함)
        sendSearchResult(chatId, keyword, result);

    } catch (Exception e) {
        log.error("[Telegram] Search failed for keyword: {}", keyword, e);
        sendSimpleMessage(chatId, "검색 중 오류가 발생했습니다. 다시 시도해주세요.");
    }
}
```

### 2. 검색 결과 포맷팅

```java
private void sendSearchResult(Long chatId, String keyword, BookSearchResult result) {
    // 헤더 메시지
    StringBuilder header = new StringBuilder();
    header.append("📚 **\"").append(keyword).append("\"** 검색 결과\n\n");

    // AI 추천 사유가 있으면 표시
    if (result.getAiResponse() != null && !result.getAiResponse().isEmpty()) {
        header.append("🤖 **AI 추천 사유**\n");
        String aiReason = result.getAiResponse().get(0).getWhy();
        if (aiReason == null || aiReason.isBlank()) {
            aiReason = "-";
        } else if (aiReason.length() > 300) {
            aiReason = aiReason.substring(0, 300) + "...";
        }
        header.append("💬 ").append(aiReason).append("\n\n");
    }

    sendSimpleMessage(chatId, header.toString());

    // 추천 도서 목록
    List<BookSearchResponse> books = result.getBooks().getContent();
    // ... 도서별 포맷팅
}
```

### 3. 도서 정보 포맷팅

```java
private void sendBookWithScore(Long chatId, int index, BookSearchResponse book) {
    StringBuilder bookInfo = new StringBuilder();

    // 순번과 제목
    bookInfo.append(index).append(". **").append(book.getTitle()).append("**\n");
    bookInfo.append("📖 ").append(book.getAuthorName()).append("\n");

    // 출판사
    if (book.getPublisherName() != null) {
        bookInfo.append("🏢 ").append(book.getPublisherName()).append("\n");
    }

    // 검색 점수 정보
    if (book.getSimilarity() != null && book.getSimilarity() > 0) {
        bookInfo.append(String.format("🎯 유사도: %.2f%%\n", book.getSimilarity() * 100));
    }
    if (book.getRrfScore() != null && book.getRrfScore() > 0) {
        bookInfo.append(String.format("📊 RRF 점수: %.2f\n", book.getRrfScore()));
    }

    // 도서 상세 링크
    bookInfo.append("🔗 [상세 보기](http://localhost:8080/books/").append(book.getId()).append(")\n");

    // 이미지가 있으면 이미지 전송, 아니면 텍스트만 전송
    if (book.getImageUrl() != null && !book.getImageUrl().isBlank()) {
        sendBookImage(chatId, book.getImageUrl(), bookInfo.toString());
    } else {
        sendSimpleMessage(chatId, bookInfo.toString());
    }
}
```

### 4. 이미지 전송

```java
private void sendBookImage(Long chatId, String imageUrl, String caption) {
    try {
        SendPhoto photo = SendPhoto.builder()
            .chatId(chatId)
            .photo(new InputFile(imageUrl))
            .caption(caption)
            .parseMode("Markdown")
            .build();

        this.execute(photo);
    } catch (TelegramApiException e) {
        log.error("[Telegram] Failed to send image, sending text instead", chatId, e);
        // 이미지 전송 실패 시 텍스트로 대체
        sendSimpleMessage(chatId, caption);
    }
}
```

---

## 메시지 포맷 예시

### 검색 결과 (AI 추천 있음)

```
📚 **"해리포터"** 검색 결과

🤖 **AI 추천 사유**
💬 해리포터 시리즈는 마법 세계를 배경으로 한 판타지 소설로, 해리가 호그와트 마법학교에서 친구들과 함께 다양한 모험을 하며 성장하는 이야기를 담고 있습니다. 영웅의 여정, 우정, 모험, 용기 등의 주제를 다루고 있습니다.

**검색된 도서 (3개)**

1. **해리포터와 마법사의 돌**
   📖 조앤 K. 롤링
   🏢 문학수침
   🎯 유사도: 85.00%
   📊 RRF 점수: 0.0350
   🔗 [상세 보기](http://localhost:8080/books/1)

2. **해리포터와 비밀의 방**
   📖 조앤 K. 롤링
   🏢 문학수침
   🎯 유사도: 82.50%
   📊 RRF 점수: 0.0330
   🔗 [상세 보기](http://localhost:8080/books/2)
```

### 검색 결과 (AI 추천 없음)

```
📚 **"주식 투자"** 검색 결과

🤖 **AI 추천 사유**
💬 -

**검색된 도서 (5개)**

1. **주식 투자의 정석**
   📖 피터 린치
   🏢 한빛비아
   🎯 유사도: 78.00%
   📊 RRF 점수: 0.0280
   🔗 [상세 보기](http://localhost:8080/books/10)
```

---

## 구현 순서

### Phase 1: 기본 RAG 검색 연동

1. `LibraryTelegramBot` 클래스 생성
2. `BookSearchService` 연동 (SearchType.RAG)
3. 검색 결과 전송 테스트

### Phase 2: 결과 포맷팅

1. AI 추천 사유 포맷팅
2. 도서 정보 포맷팅 (제목, 저자, 출판사, 점수)
3. 이미지 전송

### Phase 3: Command 처리

1. Command 분기 로직 구현
2. 일반 텍스트 검색 처리
3. 에러 메시지 정의

---

## 테스트 시나리오

### 1. 기본 키워드 검색

```
User: 해리포터
Bot: 📚 **"해리포터"** 검색 결과
     🤖 AI 추천 사유: ...
     **검색된 도서 (3개)**
     ...
```

### 2. 자연어 검색

```
User: 마법사와 관련된 책
Bot: 📚 **"마법사와 관련된 책"** 검색 결과
     ...
```

### 3. Command 검색

```
User: /search 주식 투자
Bot: 📚 **"주식 투자"** 검색 결과
     ...
```

---

## 학습 과제

### 필수 구현 사항

1. [ ] `LibraryTelegramBot` RAG 검색 연동
2. [ ] AI 추천 사유 포맷팅
3. [ ] 도서 정보 포맷팅 (이미지, 점수, 링크)
4. [ ] `/search <키워드>` Command 처리
5. [ ] 일반 텍스트 검색 처리

### 도전 과제

1. [ ] 검색 결과 없을 경우 메시지 정의
2. [ ] 자연어 검색 처리 개선
3. [ ] 에러 메시지 포맷 정의

---

## 참고 자료

- [Telegram Bot API: SendMessage](https://core.telegram.org/bots/api#sendmessage)
- [Telegram Bot API: SendPhoto](https://core.telegram.org/bots/api#sendphoto)
- [MarkdownV2 스타일 가이드](https://core.telegram.org/bots/api#markdownv2-style)

---

## 다음 단계

- [Step 5-3: 사용자 피드백 수집 - Entity & Repository](./03.feedback-entity-repository.md)
